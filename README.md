# it项目管理实验3

- 将负责的代码文件创建，然后将代码复制到文件中，详见md.
- 先`git checkout dev`改到开发分支
- 再`git pull`拉取
- 然后`git checkout -b <feature+学号+负责功能>`创建并切换到你的分支
- 然后`git add .`
- 然后`git commit -m "学号+完成xx功能"`
- 然后`git push origin 你的分支名称`